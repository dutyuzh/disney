/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['static.wikia.nocookie.net'], // Allowed domains
  },
};
